const TEXT_ATTRIBUTE = "TextAttribute";
const LOCATION_ATTRIBUTE = "LocationAttribute";
const IMAGE_ATTRIBUTE = "ImageAttribute";
const AUDIO_ATTRIBUTE = "AudioAttribute";

export const MetaDataType = {
  TEXT_ATTRIBUTE,
  LOCATION_ATTRIBUTE,
  IMAGE_ATTRIBUTE,
  AUDIO_ATTRIBUTE,
};

const LOCATION = "location";
const DEFINITION = "definition";

export const Constant = {
  LOCATION,
  DEFINITION,
};
